[server]
enableCORS = false
enableXsrfProtection = false
enableWebsocketCompression = false
headless = true
maxUploadSize = 200
fileWatcherType = "none"

# Keep the root app file path since Render runs from the repository root.
# The port is set by Render through the $PORT variable in the start command.